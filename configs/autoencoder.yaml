schema_version: 1

model:
  latent_dim: 64
  base_channels: 32

loss:
  l1_weight: 0.8
  ssim_weight: 0.2
  ssim_window_size: 7
  coin_roi_weight: 1.0

training:
  seed: 20260714
  epochs: 12
  batch_size: 32
  learning_rate: 0.0003
  weight_decay: 0.00001
  early_stopping_patience: 8
  num_workers: 0
  amp: true
  gradient_clip_norm: 1.0
  checkpoint_every_epochs: 1
  allow_diagnostic_split: true

evaluation:
  coin_red_min: 220
  coin_green_min: 170
  coin_blue_max: 40
  coin_min_pixels: 2
  coin_roi_padding: 2
